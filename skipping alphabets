import java.util.*;
import java.io.*;
class skippingnumbers
{
   public static void main(String args[])
   {
     Scanner sc = new Scanner(System.in);
     System.out.println("enter the m value:");
     int m = sc.nextInt();
     System.out.println("enter the n value:");
     int n = sc.nextInt();
     if (m<=0 || n<=0 || m==n)
     {
        System.out.println("values should be less than are equal to zero are not same:");
     }
     else if (n < m )
     {
        System.out.println("last number is greater than first number");
     }
     else
     {
        System.out.println("enter the number of values to skip:");
        int k = sc.nextInt();
        int i;
        for (i=m+k;i<=n;i++)
        {
           System.out.println(i);
        }
     }
   }
}
